/*
 Write a program that takes in a string containing '(' and ')'
 and return whether that string is a bracket matched string
 (For each closing bracket there's a corresponding open
 bracket on it's left)
 print 'YES' if it is,
 print 'NO' if it isn't
*/
int main(int argc, char const *argv[]) {
  /* code */
  return 0;
}
